import 'package:flutter/material.dart';

class SidebarCollapseButton extends StatelessWidget {
  const SidebarCollapseButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
